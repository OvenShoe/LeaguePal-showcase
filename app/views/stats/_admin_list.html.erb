<ul id="<%= dom_id(game, :stats_list) %>" class="space-y-2">
  <% editable = local_assigns.fetch(:can_edit, false) %>
  <% stats.each do |stat| %>
  <%= render "stats/admin_stat", stat: stat, game: game %>
  <% end %>
</ul>
