<div class="bg-white dark:bg-neutral-800 border border-black/10 dark:border-white/10 rounded-xl shadow-xs overflow-hidden drop-shadow-xl">
  <div class="px-6 py-5 flex flex-col items-center text-center gap-4">
    <!-- Time & Date -->
    <div>
      <p class="text-sm font-bold text-green-600"> 
        <%= @start_time %></p>
      <p class="text-sm text-neutral-500 dark:text-neutral-400"><%= @date  %></p>
    </div>

    <!-- Teams -->
    <div class="flex items-center justify-center gap-8 w-full">
      <div>
        <%# Team_1 Score Here %>
        <% if @team_1_score.present? %>
          <%= @team_1_score %>
        <% else %>
          <h2>3</h2>
        <% end %>
      </div>
      <!-- Home Team -->
      <div class="flex flex-col items-center gap-2">
        <div class="w-16 h-16 rounded-full bg-neutral-200 dark:bg-neutral-600"></div>
        <span class="text-sm font-medium text-neutral-900 dark:text-white">        
          <%= @team_1.respond_to?(:name) ? @team_1.name : @team_1 %>
        </span>
      </div>

      <!-- VS -->
      <span class="text-sm text-neutral-500 dark:text-neutral-400">vs.</span>

      <!-- Away Team -->
      <div class="flex flex-col items-center gap-2">
        <div class="w-16 h-16 rounded-full bg-neutral-200 dark:bg-neutral-600"></div>
        <span class="text-sm font-medium text-neutral-900 dark:text-white">   
            <%= @team_2.respond_to?(:name) ? @team_2.name : @team_2 %>
        </span>
      </div>
      <div>
        <%# Team_2 Score here %>
        <% if @team_2_score.present? %>
          <%= @team_2_score %>
        <% else %>
          <h2>3</h2>
        <% end %>
      </div>
    </div>

    <!-- Venue -->
    <p class="text-sm text-neutral-500 dark:text-neutral-400">Location</p>
  </div>
</div>

